{
  "setup-worktree": [
    "npm install",
    "echo ''",
    "echo '✅ Dependencies installed'",
    "echo ''",
    "if [ ! -f .clasp.json ] && [ -f .clasp.json.example ]; then cp .clasp.json.example .clasp.json && echo '✅ Created .clasp.json from example (update scriptId if needed)'; fi",
    "if [ ! -f .env ] && [ -f .env.example ]; then cp .env.example .env && echo '✅ Created .env from example'; fi",
    "if [ -f .clasp.json ] && [ -f .env ]; then echo '✅ Configuration files ready'; fi",
    "if [ ! -f ~/.clasprc.json ]; then echo '⚠️  Not authenticated with Google. Run \"npm run clasp:login\" when ready'; else echo '✅ Google authentication found'; fi",
    "echo ''",
    "echo '🎉 Worktree setup complete!'"
  ]
}

