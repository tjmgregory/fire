{
  "setup-worktree": [
    "npm install",
    "echo ''",
    "echo '✅ Dependencies installed'",
    "echo ''",
    "if [ ! -f .clasp.json ] && [ -f .clasp.json.example ]; then echo '⚠️  .clasp.json not found. Copy .clasp.json.example to .clasp.json and configure your script ID'; fi",
    "if [ ! -f .env ] && [ -f .env.example ]; then echo '⚠️  .env not found. Copy .env.example to .env and configure your settings'; fi",
    "if [ -f .clasp.json ] && [ -f .env ]; then echo '✅ Configuration files found'; fi",
    "echo ''",
    "echo '📝 Optional next steps:'",
    "echo '  • Run \"npm run setup\" for interactive configuration'",
    "echo '  • Run \"npm run clasp:login\" to authenticate with Google'",
    "echo '  • Run \"npm run deploy\" to push code to Apps Script'"
  ]
}

